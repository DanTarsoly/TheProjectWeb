import Layout from '../../components/Layout';
import RegisterComp from '../../components/account/Register';

const Register: React.FC = () => (
  <Layout>
    <RegisterComp/>
  </Layout>
);

export default Register;